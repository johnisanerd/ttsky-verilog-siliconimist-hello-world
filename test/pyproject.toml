[project]
name = "test_the_siliconimist_chip1"
version = "0.1.0"
description = "Test rig for the Siliconimist Chip1"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cocotb>=2.0.1",
    "pytest>=9.0.3",
]
